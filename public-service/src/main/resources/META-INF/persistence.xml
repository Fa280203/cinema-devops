<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="publicPU" transaction-type="RESOURCE_LOCAL">
        <!-- Classe Entity -->
        <class>com.votreorg.cinema.public.entity.Public</class>
        <!-- Provider (optionnel, par défaut Hibernate dans la plupart des cas) -->
        <properties>
            <!-- On n’y met pas les JDBC, on les injecte via JPAUtil -->
        </properties>
    </persistence-unit>
</persistence>
